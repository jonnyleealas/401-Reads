# Jonathon Lee
## Javascript 401
## Read 17


#
Given the examples of front-end events such as button click, window resize, form submit, etc, what are some examples of back-end events?
```
raise(), emit(), trigger().
```
# 
Why are events sometimes better than asynchronous actions with callbacks?


```
Events are independent and don't have to worry about being timed out. 

```
#
What does an EventEmitter instance do?
```
An eventemitter is a class and the instance of that class is an object from that class.

```
#
When is a program’s call stack, event queue, and event loop active?

```
When something refers to an event or when an event happens.
```
#

## Document the following Vocabulary Term
```
- Observer Pattern: A design patter in which an object notifies when a state has been changed.
- Listener: A function that waits for an event to occur.
- Event Handler: Scripts that execute when an event occurs.
- Event Driven Programming: Programs flow is determined by events happening.
- Event Loop: Waits for and dispatches events and sends them to the call stack.
- Event Queue: A repo where events from an app are held prior to being processed.
- Call Stack: Keeps track of its place in a script that call functions.
- Emit/Raise/Trigger: Event methods 
- Subscribe: Connection between subscriber and publisher.
- database: A database program is the heart of a business information system and provides file creation, data entry, update, query and reporting functions. {pcmag.com}

```
</br>
Which 3 things had you heard about previously and now have better clarity on?

```
Events driven programming, Aouth and Authorization. Also talking to the imaginary duck to try and understand or find solutions to problems. I learned that the brain needs to be stimulated in different ways for memory and thought process.
```
#
Which 3 things are you hoping to learn more about in the upcoming lecture/demo?

```
How to use events, different problem solving, and learning techniques.

```
#
What are you most excited about trying to implement or see how it works?

```
More css.

```

